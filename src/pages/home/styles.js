import styled from "styled-components";

export const Container = styled.main`
  background: rgba(0, 0, 0, 0.5);
  width: 100%;
  padding: 2rem;
  max-width: 1200px;
  margin: 40px auto;
  border-radius: 20px;
`